.email_delivery_container.container
  %header
    .from= delivery.message.user.professional_name
    .to= delivery.student.full_name
  .email_delivery_body
    = delivery.message_body
  - if delivery.parent.spanish_speaking?
    .translation_feedback
      = translation_feedback_message_for_delivery(delivery)
  %footer
    %time= t 'parental.web.sent', time: delivery.message.created_at.strftime('%b %e @ %l:%M%P')