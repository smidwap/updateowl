%section.messages{id: "user_#{@user.id.to_s}_last_week_messages"}
  = render "header"
  
  .filter_alert These are the updates you sent <b>Last Week</b>. You can #{link_to "view all updates", user_messages_path(@user)} instead

  = render "shared/message_list", messages: @user.messages.last_week