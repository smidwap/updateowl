.event_container.checked_message
  %header
    .subject
      #{event.message.student.full_name}'s parent checked #{link_to truncate(event.message.body, length: 40), message_path(event.message), remote: true}
    %time= time_or_date_format(event.time)